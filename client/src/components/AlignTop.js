import React from "react";
//site-wide styling convention for dollar signs
const AlignTop = ({ content }) => (
  <span className="mr-0 align-top font-weight-bold">{content}</span>
);

export default AlignTop;
